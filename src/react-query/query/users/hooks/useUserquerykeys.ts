import { USER_QUERY_KEYS } from "../enum"

export const useUsersquerykeys = () => {
    return {list:USER_QUERY_KEYS.list}
}

export const useUserquerykeys = () => {
    return {user:USER_QUERY_KEYS.user}
}